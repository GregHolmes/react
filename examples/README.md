# Examples

| Example | Description |
|---------|-------------|
| [`basic/`](basic/) | Minimal AgentProvider + useAgentState + useAgentConversation |

## Adding Examples

Each example lives in its own directory with:

- `App.tsx` -- the example component
- `README.md` -- what it demonstrates and how to run it
